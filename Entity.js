class Entity {
  // x;
  // y;
  constructor(x, y, width, height) {
    this.x = x;
    this.y = y;
    // width = width;
    // height = height;

  }

  static couldBeHere(ent, ents){
    return 1;
  }

  static checkCollisionsBetweenEntities(ent1, ent2){

  }
}
